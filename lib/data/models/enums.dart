enum OwnerShip { Owner, Lease, Sharing }

enum FarmerType { Commercial, Subsistence }

enum CropType { Vegetable, Fruit, Grain }

enum LiveStockType { Aquatic, Poultry, Cattle, Goat }

enum Gender { MALE, FEMALE }

enum ApplicationStatus { ACCEPTED, INPROGRESS, REJECTED }
